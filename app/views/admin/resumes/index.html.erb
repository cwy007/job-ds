<!-- <div class="container">
  <div class="row">
    <div class="col-md-10 col-md-offset-1">
      <h1 class="text-center">投递到 - <%=link_to @job.title, admin_job_path(@job) %> - 的简历</h1>

      <hr/>
      <%= render partial: "resumes", collection: @resumes, as: :resume %>
    </div>
  </div>
</div> -->

<h1 class="text-center">投递到 - <%=link_to @job.title, admin_job_path(@job) %> - 的简历</h1>

<hr/>
<%= render partial: "resumes", collection: @resumes, as: :resume %>
